"""Service layer (Group E): domain workflows orchestrating repositories.

Services depend only on Types (`src.types`), Config (`src.config`), and
Repository (`src.repositories`) -- never on the API or UI layers, per
`.claude/architecture.md`.
"""
